models:
  - name: stg_sellers
    columns:
      - name: seller_id
        tests: [not_null, unique]

      - name: zip_prefix
        tests: [not_null]

      - name: seller_city
        tests: [not_null]

      - name: seller_state
        tests:
          - not_null
